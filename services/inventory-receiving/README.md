# inventory-receiving

Backend microservice skeleton for KIS Nexus.

## Local run

go run ./cmd/api

## Default endpoints

- GET /healthz
- GET /readyz
- GET /receipts
- POST /receipts
- POST /receipts/{id}/discrepancy
