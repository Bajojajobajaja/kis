# crm-contacts

Backend microservice skeleton for KIS Nexus.

## Local run

go run ./cmd/api

## Default endpoints

- GET /healthz
- GET /readyz
- GET /clients
- POST /clients
- GET /contacts
- POST /contacts
- GET /interactions
- POST /interactions
