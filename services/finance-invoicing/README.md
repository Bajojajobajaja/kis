# finance-invoicing

Backend microservice skeleton for KIS Nexus.

## Local run

go run ./cmd/api

## Default endpoints

- GET /healthz
- GET /readyz
- GET /invoices
- POST /invoices
- GET /payments
- POST /payments
