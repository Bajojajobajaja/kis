# service-parts-usage

Backend microservice skeleton for KIS Nexus.

## Local run

go run ./cmd/api

## Default endpoints

- GET /healthz
- GET /readyz
- GET /workorders/{id}/parts
- POST /workorders/{id}/parts
