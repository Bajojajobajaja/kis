# PostgreSQL Backups

This directory is the repo-local destination for PostgreSQL SQL dumps created by
`scripts/ops/backup-postgres.ps1`.

Use it when you explicitly want to version a dev/test database snapshot together
with code changes.
